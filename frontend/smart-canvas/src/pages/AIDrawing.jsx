import React from "react";
export default function AIDrawing(){
  return <div>
    <h2>AI Drawing (coming soon)</h2>
    <p>Sketch something here — this will call /api/recognize when M2 is ready.</p>
  </div>
}
